# Telegram bot: create via @BotFather, then /setprivacy -> Disable so the
# bot sees every message in a topic, not just @-mentions.
TELEGRAM_BOT_TOKEN=

# Your numeric user_id is the gate: the bridge answers you in whatever chat
# you speak in (any group, any topic, a DM). The chat_id is the "home" chat --
# one candidate (activity-ranked, not privileged) for where an MCP
# session_create without an explicit chat_id opens its topic. Find both by
# sending one message in any topic and checking
# https://api.telegram.org/bot<token>/getUpdates
TELEGRAM_CHAT_ID=
TELEGRAM_ALLOWED_USER_ID=

# zcode runtime (see README.md for how this was obtained/installed)
ZCODE_NODE_BIN=
ZCODE_BIN=
ZCODE_WORKSPACE_DIR=

# Model used for newly-created topic sessions. Switch anytime with
# ZCODE_DEFAULT_MODEL=zai/glm-5.2 (or glm-5.3-flash).
ZCODE_DEFAULT_MODEL=zai/glm-5.3

# yolo = auto-approve at the session level. AUTO_APPROVE_PERMISSIONS is a
# second, independent safety net: if an interaction/requestPermission still
# arrives despite yolo mode, it's auto-approved with a non-blocking notice
# posted to the topic. Set to false to fall back to interactive
# Approve/Deny inline-keyboard prompts instead.
ZCODE_DEFAULT_MODE=yolo
AUTO_APPROVE_PERMISSIONS=true

# How long an interactive permission prompt waits before auto-denying
# (only relevant when AUTO_APPROVE_PERMISSIONS=false).
PERMISSION_TIMEOUT_MS=600000

# Streaming: the reply-to-be lands on the turn's placeholder message and is
# edited in place, at most once per this interval (the 20 msg/min group cap
# is not a constraint; this is the agreed pacing rule).
STREAM_EDIT_INTERVAL_MS=5000

# How often the placeholder's elapsed-time counter gets nudged even with NO
# new content, so one long tool call (a VM boot, a slow test run, ...) can't
# freeze it for the whole stretch and make a genuinely-running turn look
# dead. 0 disables the pulse (old behavior: only real events cause an edit).
STREAM_HEARTBEAT_MS=60000

# Turn-progress presentation. 'messages': one Telegram message per
# milestone (the model's between-tool narration), only the last one live-
# updating, with the steps (tool calls) listed inside each; earlier
# milestones freeze into a checkmark record. 'preview': the classic single
# placeholder streaming the reply text. 'off': placeholder only, final
# delivery at completion.
STREAM_PROGRESS=messages

# How long a posted model question (AskUserQuestion -> inline buttons) waits
# for a tap before being auto-declined so the turn keeps moving.
USER_INPUT_TIMEOUT_MS=600000

# /file upload cap in MB (Telegram bots accept up to 50).
MAX_FILE_MB=45

# Inbound (user-sent) document cap in MB; 20 is Telegram's hard bot download
# limit, so anything larger can sit in chat but never be fetched.
MAX_INBOUND_FILE_MB=20

# Circuit breaker for blocking TaskOutput waits: a turn whose current tool
# is TaskOutput(block=true) for longer than this is auto-interrupted; its
# background tasks keep running (completion notifications still arrive).
# Ends the observed-in-real-life pattern of an agent camping through
# 10-minute TaskOutput timeouts for hours. 0 disables.
TASK_BLOCK_LIMIT_MS=900000

# Serve the MCP gateway on a unix domain socket (newline-delimited
# JSON-RPC; the file is chmod'd 0600 -- the permissions ARE the
# authentication, so keep the parent directory owner-only). Production
# transport for the junior-agent MCP; takes precedence over nothing -- set
# either or both this and MCP_HTTP_PORT.
MCP_UNIX_SOCKET=

# Turn-timeout watchdog. DISABLED by default: the old
# 20-minute default killed real, merely-slow turns; /stop is the designated
# escape hatch for a wedged topic. Set a duration (ms) to re-arm the sweep.
TURN_TIMEOUT_MS=0

# How long a redeploy (SIGTERM/SIGINT) waits for in-flight turns to finish
# naturally before falling back to interrupt-and-notify. While draining, new
# messages are queued (with an explanatory notice) instead of starting a
# fresh turn -- the bridge stays responsive throughout, it just stops
# admitting new work. Set to 0 to skip draining and restart immediately
# (the old behavior: every in-flight turn gets interrupted right away).
SHUTDOWN_DRAIN_MS=1500000

# Optional second backend: a topic can run on Codex (`codex app-server`)
# instead of zcode (see /backend in Telegram, or session_create's `backend`
# argument over MCP). Off unless CODEX_HOME is set -- a deployment that
# never sets these behaves exactly as before this backend existed.
#CODEX_BIN=codex
#CODEX_HOME=/path/to/a/codex/login (contains auth.json; never committed)
# gpt-5.6-terra is the junior/default tier (Codex's Sonnet-equivalent) --
# left unset it still defaults to this, not to Codex's own server-side
# default (gpt-6-astra, its newest and most expensive model). This is the
# model an MCP-driven Codex session gets when no model is chosen, and it
# should name one of the CODEX_MCP_MODELS below.
#CODEX_DEFAULT_MODEL=gpt-5.6-terra
# Which models MCP callers may pick for a codex session (session_create's
# `model` argument, and model_set): a comma list, replaced wholesale, never
# extended. Unset or empty = the three everyday tiers
# gpt-5.6-luna,gpt-5.6-terra,gpt-5.6-sol. Pin the bridge to one model by
# naming one (e.g. a bridge that must spend only Terra:
# CODEX_MCP_MODELS=gpt-5.6-terra together with CODEX_DEFAULT_MODEL above).
# Anything not listed -- including gpt-6-astra, always -- is refused with an
# error naming the allowed set. Telegram's /model is NOT narrowed by this.
#CODEX_MCP_MODELS=gpt-5.6-luna,gpt-5.6-terra,gpt-5.6-sol
# Refuses `/model gpt-6-astra` in Telegram outright. Off by default --
# Telegram may switch to any model including Astra unless this is set.
# Meant for a deployment that must never spend Astra-tier usage (e.g. this
# bridge's own test bots), not a general restriction.
#CODEX_DISALLOW_ASTRA=

# A third backend, always available, needing NO config at all: an in-process
# echo (no subprocess, no credential, no real API cost). Its replies are
# always a synthetic "[mock echo] <prompt>" -- useful for testing the
# multi-backend MCP/Telegram machinery for real without spending z.ai or
# Codex quota. See /backend in Telegram, or session_create's `backend`
# argument over MCP -- there is nothing to set here to enable it.

# Which backend a brand-new topic/session uses absent an explicit choice
# ('zcode', 'codex', or 'mock'). Whichever this names is the one started
# EAGERLY at boot (the load-bearing backend -- its death takes the whole
# bridge down so the service manager restarts it); the others start lazily,
# the first time a topic actually asks for them, and their death does not.
# Left at 'zcode' so an existing deployment's behavior is unchanged unless
# this is deliberately switched. DEFAULT_BACKEND=mock needs no other env var
# at all -- a deployment that wants zero external dependencies for testing.
#DEFAULT_BACKEND=zcode
# Which backends are constructed and started EAGERLY at boot: a comma list.
# Unset = the default backend alone (so a codex-default bridge still never
# spawns `zcode app-server` at boot). Name extra backends only if this
# deployment means to serve them without a lazy first touch -- e.g. a
# bridge whose single Telegram chat lists models of both backends:
# EAGER_BACKENDS=codex,zcode. An unknown name refuses to boot.
#EAGER_BACKENDS=
# Which backends /model spans: a comma list. Unset = every backend EXCEPT
# mock (the zero-credential test double must not appear in a production
# bridge's /model, be constructed by it, or be resolvable through it); a
# bridge whose DEFAULT_BACKEND is mock -- or which eagerly starts mock via
# EAGER_BACKENDS -- is itself a test bridge and sees mock unless you name a
# list without it. Backends outside the span are never constructed by
# /model, and `backend:name` qualified refs naming them are refused as
# unknown. An unknown name refuses to boot. (/backend is unaffected: an
# operator can still switch a topic to mock by hand.)
#MODEL_BACKENDS=

# Testing seam: point at a local fake Telegram to run test/e2e.mjs against
# the whole bridge without touching the live bot. Leave unset in production.
#TELEGRAM_API_ROOT=

# Where the topic<->session store (data/sessions.json by default) and its
# lock file live. Only relevant if you need multiple independent bridge
# deployments on one host -- each needs its own STORE_PATH.
STORE_PATH=
